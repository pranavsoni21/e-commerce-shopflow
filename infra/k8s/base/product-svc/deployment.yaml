apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-svc
  namespace: shopflow
  labels:
    app: product-svc
    version: "1.0"
    part-of: shopflow
    managed-by: argo-cd

spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-svc
  template:
    metadata:
      labels:
        app: product-svc
        version: "1.0"
        part-of: shopflow
        managed-by: argo-cd

    spec:
      serviceAccountName: product-sa
      containers:
        - name: product-svc
          image: 156084119771.dkr.ecr.ap-south-1.amazonaws.com/shopflow/product:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
          - name: AWS_REGION
            value: "ap-south-1"

          # Secret Config
          - name: DATABASE_URL
            valueFrom:
              secretKeyRef:
                name: product-svc-secrets
                key: database_url

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5    # wait 5s after start before first check
            periodSeconds: 10         # check every 10s
            failureThreshold: 3       # fail 3 times before marking unready

          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15   # give app more time to fully start
            periodSeconds: 20
            failureThreshold: 3