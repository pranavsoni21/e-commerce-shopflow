apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: shopflow
  labels:
    app: frontend
    version: "1.0"
    part-of: shopflow
    managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
